// Package registry implements the project registry service.
// It manages project lifecycle (list, open, close, init, validate)
// and persists state to ~/.config/sudocode/projects.json.
package registry
